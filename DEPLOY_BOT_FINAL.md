# Финальная инструкция по обновлению бота на сервере

## ✅ Изменения отправлены в репозиторий

Изменения в коде бота отправлены в репозиторий:
- **Репозиторий:** https://github.com/SpiritWalker84/trivia-bot
- **Ветка:** `feature/leaderboard`
- **Коммит:** `9ac6942` - "Update /start command to open web interface instead of showing menu"

## 📋 Что нужно сделать на сервере

### Шаг 1: Обновить код бота

```bash
cd ~/megagames/trivia-bot
git checkout feature/leaderboard  # или ваша рабочая ветка
git pull origin feature/leaderboard
```

### Шаг 2: Пересобрать контейнер бота

```bash
cd ~/megagames/trivia-web
sudo docker compose build --no-cache bot
sudo docker compose up -d bot
```

### Шаг 3: Проверить логи

```bash
sudo docker compose logs bot --tail=50 -f
```

## 🔍 Проверка работы

1. Нажмите `/start` в боте
2. Должна появиться кнопка "🎮 Начать игру"
3. При нажатии откроется веб-интерфейс: `http://193.42.127.176/?telegram_id=YOUR_ID`
4. Веб-интерфейс покажет экран выбора типа игры

## ⚙️ Опционально: Настроить WEB_URL

Если URL веб-интерфейса отличается от `http://193.42.127.176`, добавьте в `.env` файл в `trivia-web/`:

```env
WEB_URL=http://193.42.127.176
```

Или добавьте в `docker-compose.yml` в секцию `bot`:

```yaml
bot:
  environment:
    - WEB_URL=${WEB_URL:-http://193.42.127.176}
    # ... остальные переменные
```

## 📝 Что было изменено

1. **`trivia-bot/config.py`**:
   - Добавлена переменная `WEB_URL` для настройки URL веб-интерфейса

2. **`trivia-bot/main.py`**:
   - Обновлена функция `start_command`:
     - Теперь отправляет кнопку для открытия веб-интерфейса
     - Вместо показа меню выбора режима игры
     - Сохранена поддержка приватных игр (`/start private_123`)

## 🚀 Быстрая команда (все в одном)

```bash
cd ~/megagames/trivia-bot && git pull origin feature/leaderboard && cd ../trivia-web && sudo docker compose build --no-cache bot && sudo docker compose up -d bot && sudo docker compose logs bot --tail=50
```
