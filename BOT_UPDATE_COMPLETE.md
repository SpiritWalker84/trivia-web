# Обновление бота завершено ✅

## Что было сделано:

1. ✅ Обновлен обработчик `/start` в `trivia-bot/main.py`
2. ✅ Добавлена поддержка `WEB_URL` в `trivia-bot/config.py`

## Что нужно сделать на сервере:

### 1. Обновить код бота:

```bash
cd ~/trivia-bot
git pull  # или git checkout нужной ветки
```

### 2. Настроить переменную окружения (опционально):

Если URL веб-интерфейса отличается от `http://193.42.127.176`, добавьте в `.env` файл бота:

```env
WEB_URL=http://193.42.127.176
```

### 3. Перезапустить бота:

```bash
# Если бот в Docker
sudo docker compose restart bot

# Или если бот запущен отдельно
# Остановите и запустите заново
```

## Как это работает теперь:

1. Пользователь нажимает `/start` в боте
2. Бот отправляет кнопку "🎮 Начать игру"
3. При нажатии открывается веб-интерфейс: `http://193.42.127.176/?telegram_id=123456789`
4. Веб-интерфейс показывает экран выбора типа игры:
   - Имя игрока загружается автоматически из БД
   - Две кнопки: "Тренировка с ботами" и "Игра с друзьями"
5. Пользователь выбирает тип игры и начинает играть

## Проверка:

1. Нажмите `/start` в боте
2. Должна появиться кнопка "🎮 Начать игру"
3. При нажатии откроется веб-интерфейс с выбором типа игры

## Важно:

- Обработка приватных игр (`/start private_123`) сохранена для обратной совместимости
- Пользователь автоматически создается/находится в БД при `/start`
- URL веб-интерфейса можно настроить через переменную окружения `WEB_URL`
